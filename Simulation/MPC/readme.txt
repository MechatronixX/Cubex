File of interest:

'cube_2d_simulation_model_fast_mpc' - Non linear model using "Fast mpc" solver

'MPC_Parameters' - Function that init the MPC problam and 
		   derive the Hessian matrix and all the inequaliy and
	  	   equality constrain. Return all the matrix needed for the slover

'initFastMPC'    - Short script that call the MPC_Parameters function.

